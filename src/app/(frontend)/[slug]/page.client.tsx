'use client'
import React, { useEffect } from 'react'

const PageClient: React.FC = () => {
  /* Previously forced a header theme; now a no-op */
  useEffect(() => {}, [])
  return <React.Fragment />
}

export default PageClient
